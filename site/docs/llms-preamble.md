# Session Watcher

> A local monitor that estimates when carrying an existing Claude Code
> context becomes more expensive than rebuilding it.

Session Watcher provides a dashboard, statusline, restart-cost metrics,
and handoff tooling for long-running coding sessions.

Demo: `npx -y @nomadop/session-watcher demo`
